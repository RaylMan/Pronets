<Page x:Class="Pronets.Viev.MainWindows.Pages.ReceiptDocumentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pronets.Viev.MainWindows.Pages"
      xmlns:vm="clr-namespace:Pronets.VievModel.MainWindows.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReceiptDocumentPage">

    <Page.DataContext>
        <vm:ReceiptDocumentPageVM/>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid x:Name="Docunents" Margin="5" Grid.Row="1" Grid.Column="0" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Path=ReceiptDocuments}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер Документа" Binding="{Binding Path=DocumentId}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Path=ClientId}" />
                <DataGridTextColumn Header="Приемщик" Binding="{Binding Path=InspectorId}" />
                <DataGridTextColumn Header="Дата" Binding="{Binding Path=Date}" />
                <DataGridTextColumn Header="Заметки" Binding="{Binding Path=Note}" />
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Открыть"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>

